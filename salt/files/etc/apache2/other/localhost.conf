<VirtualHost *:80>
	ServerName localhost

	DocumentRoot {{ pillar.get("apache_localhost_dir") }}

	<Directory "{{ pillar.get("apache_localhost_dir") }}">
		Options FollowSymLinks Multiviews
		MultiviewsMatch Any
		AllowOverride All
		Require all granted
	</Directory>

</VirtualHost>
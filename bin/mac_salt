#!/bin/bash

MACSALT=$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )
ARGCOUNT=`wc -w <<< $@`
if [ $ARGCOUNT -lt 2 ] && [ $@ = "update" ]; then
	cd $MACSALT/..
	git pull
else
	sudo -H -E salt-call --local --file-root=$MACSALT/../salt --pillar-root=$MACSALT/../pillar $@
fi
